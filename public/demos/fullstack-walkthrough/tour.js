const steps=[...document.querySelectorAll('[data-step]')];let current=0;const previous=document.getElementById('previous'),next=document.getElementById('next'),progress=document.getElementById('progress');function render(){steps.forEach((step,index)=>{step.hidden=index!==current});progress.textContent=`Step ${current+1} of ${steps.length}`;previous.disabled=current===0;next.disabled=current===steps.length-1;}document.getElementById('controls').hidden=false;previous.addEventListener('click',()=>{current=Math.max(0,current-1);render()});next.addEventListener('click',()=>{current=Math.min(steps.length-1,current+1);render()});document.getElementById('show-all').addEventListener('click',()=>{steps.forEach(step=>step.hidden=false);document.getElementById('controls').hidden=true});render();
